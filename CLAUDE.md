## Critical rules
Do not ever use emojis unless explicitly instructed to
All pip dependencies should be installed to the virtual environment (venv).  All python modules and scripts should be run from the venv.  Never use the system's base python interpreter to install any dependencies or run any python modules